{
    "extends": "./tsconfig.json",
    "include": ["vite.config.ts", "eslint.config.ts"],

    "compilerOptions": {
        // Modules
        "types": ["@types/node"],

        // Language and Environment
        "lib": ["ESNext"],
        "target": "ESNext"
    }
}
