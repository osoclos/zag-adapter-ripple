{
    "extends": "./tsconfig.lib.json",
    "include": ["tests"],

    "compilerOptions": {
        // Modules
        "types": ["vite/client", "./lib/ripple-shims.d.ts"],

        // Emit
        "declaration": false,
        "emitDeclarationOnly": false,
        "noEmit": true
    }
}
